<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/defaultMargin"
    android:background="?windowBackgroundDark"
    android:orientation="vertical"
    >

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?selectableItemBackground"
        android:drawableLeft="?iconArrowDown"
        android:gravity="center"
        android:padding="@dimen/defaultMarginSmall"
        android:textColor="?spoilerColorTitle"
        tools:ignore="RtlHardcoded"
        tools:text="Заголовок спойлера"
        />

    <TextView
        android:id="@+id/content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:padding="@dimen/defaultMarginSmall"
        android:visibility="gone"
        tools:text="Содержимое спойлера"
        tools:visibility="visible"
        />

</LinearLayout>